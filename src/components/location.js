import React from 'react'
import {Text} from '@chakra-ui/core'
export default function Location({children}){
    return(
        <Text marginLeft={['','','3']}>
            {children}
        </Text>
    )
}